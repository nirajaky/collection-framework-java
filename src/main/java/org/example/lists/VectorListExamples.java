package org.example.lists;

public class VectorListExamples {

    // Vector is very much similar to ArrayList
    // Except Vector is thread Safe Class, means it has synchronized methods making it to be used by
    // multiple threads without loosing its integrity

    // Vector suffers a slight performance penalty compared to ArrayList due to synchronization
}
